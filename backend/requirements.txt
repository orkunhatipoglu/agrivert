# Install torch/torchvision FIRST from the CUDA index (same caveat as the ML
# repo's requirements.txt — plain PyPI can silently give you a CPU-only build):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # multipart upload for POST /diagnoses
pydantic>=2.8
pydantic-settings>=2.4
email-validator>=2.2           # pydantic EmailStr
sse-starlette>=2.1             # GET /diagnoses/{id}/stream

# --- Queue ---
celery[redis]>=5.4
redis>=5.0

# --- Firebase ---
firebase-admin>=6.5
google-cloud-firestore>=2.16

# --- Inference ---
# The backend imports predict.py/data.py from the ML repo rather than
# reimplementing preprocessing, so it needs the same inference-side deps.
torch>=2.4
torchvision>=0.19
albumentations>=1.4.15
opencv-python-headless>=4.9
numpy>=1.24
Pillow>=10.0

# --- Dev/test ---
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
